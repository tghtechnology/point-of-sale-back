class Venta {
    constructor(detalles, tipoPago, impuestoId, descuentoId, subtotal, total){
        this.detalles = detalles;
        this.tipoPago = tipoPago;
        this.impuestoId = impuestoId;
        this.descuentoId = descuentoId;
        this.subtotal = subtotal;
        this.total = total;
    }
}