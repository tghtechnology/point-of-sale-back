class Impuesto {
    constructor(id, nombre, tasa, tipo_impuesto, estado) {
        this.setId(id);
        this.setNombre(nombre);
        this.setTasa(tasa);
        this.setTipoImpuesto(tipo_impuesto);
        this.setEstado(estado);
    }
}